# Configuration de la base de données
# Par défaut, SQLite est utilisé (aucune configuration nécessaire)

# Pour utiliser SQLite (défaut - aucune action requise):
# DATABASE_URL=sqlite:///./project_db.sqlite

# Pour utiliser MySQL (décommenter et configurer):
# DATABASE_URL=mysql+pymysql://user:password@localhost:3306/project_db

# Pour utiliser PostgreSQL (décommenter et configurer):
# DATABASE_URL=postgresql://user:password@localhost:5432/project_db

# Autres variables d'environnement
# DEBUG=true
# LOG_LEVEL=INFO
